# Development & test dependencies (not installed in production image)
-r requirements.txt

pytest==8.2.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0
httpx==0.27.0        # already in main requirements, listed for clarity
anyio==4.4.0
